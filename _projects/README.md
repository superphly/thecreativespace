# Projects

One markdown file per project — the ventures and events that grew out of
the Space (Always Creative, BIL Conference, Desired Hearts, Hashtags.org,
RefreshBCS, Rock The Republic). Rendered at `/projects/<slug>/`.

`photo:` in frontmatter is the project logo; the body is the description.
