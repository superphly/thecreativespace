# Media

The site's original WordPress uploads (2009–2012), kept at their original
`YYYY/MM/` paths. Content references them as `/media/YYYY/MM/<file>`.
Resident and project photos live here alongside inline page images.
